
mysql -u root < schema.sql

mysql -u root < datadump.sql

mysql -u root < baseViews.sql

mysql -u root < team.sql
mysql -u root < conf.sql
mysql -u root < bowl.sql
mysql -u root < year.sql
mysql -u root < rank.sql
